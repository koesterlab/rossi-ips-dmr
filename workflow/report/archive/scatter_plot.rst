Methylation level at a curated set of pluripotency and germ-layer marker CpGs, one panel per marker. Each point is one sample, split by whether the sample belongs to the marker's target cell fate or not, and colored by germ layer; methylation values are oriented so that higher always means "more marker-like". Panel subtitles report the summed methylation per germ layer for that marker.
